interface:
  display_name: "Architecture Standards"
  short_description: "Design and enforce proportionate architecture"
  default_prompt: "Use $architecture-standards to design or implement this change with clear ownership, proportionate boundaries, explicit contracts, and enforceable fitness functions."

policy:
  allow_implicit_invocation: true
