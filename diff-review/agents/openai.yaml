interface:
  display_name: "Diff Review"
  short_description: "Review diffs for bugs, safety, and maintainability"
  default_prompt: "Use $diff-review to review my current git diff for bugs, regressions, devex issues, feature-gate leaks, maintainability regressions, analyzer-policy issues, architecture drift, and open findings before I push."
